# Note: full-automatic kernel-build doesn't work, ramdisk + kernel cooking needs superr-kitchen
# AIK (with same steps like in superr does NOT lead to booting kernel)

### Linar 7.2 TC

## Linaro 7.3 TC with binutils 2.30 has issues during linking, waiting for newer ld

# (Linaro?) 7.x toolchain has issues with SMS (modulo-sched) flags and quits with internal compiler error


###make oldconfig

export USE_CCACHE=1

export CACHE_DIR=~/.ccache


export KBUILD_BUILD_USER=zacharias.maladroit
export KBUILD_BUILD_HOST=BuildHost

export ARCH=arm64
export PATH=~/android/toolchains/gcc-linaro-7.2.1-2017.11-x86_64_aarch64-linux-gnu/bin/:$PATH
CROSS_COMPILE=aarch64-linux-gnu-
#export CROSS_COMPILE=aarch64-linux-android-

make joan_global_com-perf_defconfig

time make -j8

# superr-kitchen steps:
# opening boot.img in superr
# deleting old boot.img-zImage
# copying over new boot.img-zImage (Image.gz-dtb)


sudo rm ~/android/superrs-kitchen/superr_v30_stock/bootimg/split_img/boot.img-zImage

sudo cp arch/arm64/boot/Image.gz-dtb ~/android/superrs-kitchen/superr_v30_stock/bootimg/split_img/boot.img-zImage



cd ~/android/superrs-kitchen/superr_v30_stock
fastboot boot boot.img



#echo "generating device tree..."
#make dtbs
#../final_files/dtbToolCM --force-v2 -o ../final_files/dt.img -s 2048 -p ./scripts/dtc/ ./arch/arm/boot/dts/

### copy zImage
#cp arch/arm64/boot/Image ../final_files/. 

	echo "okay"

	### H930
	../final_files/mkbootimg_MM --cmdline "console=ttyMSM0,115200,n8 androidboot.console=ttyMSM0 user_debug=31 msm_rtb.filter=0x37 ehci-hcd.park=3 lpm_levels.sleep_disabled=1 sched_enable_hmp=1 sched_enable_power_aware=1 service_locator.enable=1 swiotlb=2048 androidboot.configfs=true androidboot.usbcontroller=a800000.dwc3 androidboot.hardware=joan buildvariant=user androidboot.selinux=permissive" --base 0x00000000 --kernel arch/arm64/boot/Image.gz-dtb --kernel_offset 0x00008000 --ramdisk ../final_files/boot_v30_debloated_stock-oreo.img-ramdisk.cpio.gz --ramdisk_offset 0x01000000 --pagesize 4096 -o ../final_files/V30_8.0.0_stock/boot_H930.img --tags_offset 0x00000100 --second_offset 0x00f00000
